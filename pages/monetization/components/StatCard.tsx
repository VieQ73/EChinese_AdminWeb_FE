
import React from 'react';

// Một component thẻ thống kê có thể tái sử dụng cho dashboard.
const StatCard: React.FC = () => {
    return (
        <div>Stat Card Component Placeholder</div>
    );
};

export default StatCard;
